// 3. Return the lowest index at which a value should be inserted into an array once it has been sorted.

// (example: ([1,2,3,5], 1.5) should return 1 because it's greater than 1 (index 0) and less than 2 (index 1).

function insert_num (arr, val) {

    let sorted = arr.sort()

    let len = arr.length
    let max = len
    let min = 0
    let check = Math.floor(len / 2)

    let idxFind = (subArr, val) => {

    }

    // if (val < arr[check]){

    // }

}